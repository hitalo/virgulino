#ifndef _HIDER_H_
#define _HIDER_H_

#include <string.h>
#include "definitions.h"
#include "cypher_utils.h"

// proto's ::
char * hide (char * message);
char * unhide (const char * filepath);

#endif /* _HIDER_H_ */
